
# Sidecar tarball — 2.5 GB output of scripts/build-sidecar.sh, not for git
/Sidecar/bootstrap/python-runtime.tar.gz
/Sidecar/bootstrap/python-runtime.tar.gz.sha256

# Xcode archives / local build products
/build/
SoniqueBar/Resources/*.gguf

# TTS engine build artifacts (exceed GitHub 100MB limit)
tts-engine/build/
tts-engine/dist/
tts-engine/venv/
*.pkg
*.pyz
