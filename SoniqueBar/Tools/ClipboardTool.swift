enum ClipboardTool { static func execute(input: [String: Any]) async -> String { let pbcopyProcess = Process() pbcopyProcess.launchPath = "/usr/bin/pbcopy" let pbpasteProcess = Process() pbpasteProcess.launchPath = "/usr/bin/pbpaste" let output = await pbpasteProcess.run() return output } }
